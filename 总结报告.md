# 三角洲行动 · 本地改枪码助手 — 开发总结报告

**日期**：2026-08-16
**项目路径**：`E:\something\AI Agent\xinjinhe\gun-codes-app`
**类型**：独立 Tauri 桌面应用（与 NexBox 完全解耦）

---

## 一、需求背景

用户原本在 NexBox（上游 v8.0.9）内嵌「三角洲行动本地改枪码库」功能。后发现直接改动 NexBox 源码会在上游更新时丢失/冲突，决定**将该功能独立成单独小应用**，彻底摆脱上游影响。

## 二、最终交付物

| 产物 | 路径 | 大小 |
|---|---|---|
| 可执行文件 | `src-tauri\target\release\gun-codes-app.exe` | 9.17 MB |
| MSI 安装包 | `src-tauri\target\release\bundle\msi\GunCodesAssistant_1.0.0_x64_en-US.msi` | 3.04 MB |

双击 exe 即可运行，无需管理员权限，无需安装。

## 三、功能清单

1. **粘贴自动识别**：粘贴改枪码 → 自动识别并填充「枪名 + 配置名（备注）」两个字段
   - 例：`6KFCC1K08QMG6NRUK6UQC` → RM277 + 20W青春版
   - 例：`6KFCDT008QMG6NRUK6UQC` → RM277 + 55W满改红点
2. **内置识别库**：244 条前缀映射，覆盖 63 把枪、196 条带配置名（步枪/冲锋枪/射手步枪/狙击/机枪/霰弹枪/手枪/弓/左轮等全类别）
3. **学习机制**：手动保存新枪后自动记住「前缀→枪名+配置名」，同枪代码下次自动识别
4. **本地持久化**：数据存于 `local_gun_codes.json`（tauri-plugin-store），离线可用
5. **列表管理**：搜索（枪名/代码/备注）、一键复制、编辑、删除
6. **统计信息**：顶部显示记录总数 / 枪械种类数
7. **批量导入（云更新支持）**：一键粘贴整篇在线文档/群消息，自动解析全部改枪码（支持表格格式 + 「枪名-烽火地带-代码」格式），识别广告行并过滤，去重后批量保存并学习全部前缀
8. **内置库已覆盖新赛季**：352 条前缀映射 / 63 把枪（含 2026 新赛季云更新 109 条新码，识别率 100%）

## 四、识别策略（核心设计）

- **前缀规则**：三角洲改枪码前 **6 位**字母数字唯一标识枪械+配置
  - 前 4/5 位会跨枪冲突（如 `6JDG` 同时是 K437/MCX/腾龙）
  - 6 位前缀在 352 条样本、63 把枪中 **100% 唯一，0 冲突**
- **识别优先级**：学习映射 > 内置映射 > 格式启发式（DELTA- 前缀 / -XXX-001 模式）

## 五、技术栈

- **后端**：Rust + Tauri 2.11.5 + tauri-plugin-store 2.4.4（serde/serde_json/uuid）
- **前端**：React 19 + TypeScript 5.8 + Vite 6（纯手写 CSS，无 UI 框架依赖）
- **打包**：tauri build → NSIS/MSI（productName 用英文 `GunCodesAssistant` 以兼容 WiX；窗口标题仍为中文「改枪码助手」）

## 六、自查结果（全部通过）

| 检查项 | 结果 |
|---|---|
| 后端 cargo check | ✅ 编译通过 |
| 前端 tsc + vite build | ✅ 构建通过 |
| 352 条前缀唯一性 | ✅ 0 冲突 |
| 109 条新赛季云更新码识别 | ✅ 100%（109/109） |
| 批量导入解析器（真实文档测试） | ✅ 表格/烽火地带/广告过滤/枪名继承 全部正确 |
| 240 条真实样本码识别正确性 | ✅ 100% 正确 |
| 命令签名 ↔ 前端 invoke 参数 | ✅ 全部匹配 |
| exe 启动冒烟测试 | ✅ 启动正常，GUI 加载成功 |
| NexBox 回滚 | ✅ git status 干净，HEAD=35feaa0 |

## 七、NexBox 处理

- NexBox 内旧改动（lib.rs +6 行、App.tsx +3 行、两个新文件）**已全部回滚/删除**
- NexBox 回到纯净上游 v8.0.9（`35feaa0`），后续 `git pull` 上游零冲突

## 八、已知事项 / 后续建议

1. **GitHub PAT 轮换**：fork 时使用的 PAT 仍存在于仓库 remote，建议轮换（`gh auth` 或 GitHub 设置页）
2. **可选增强**：
   - 同枪不同配置分组折叠显示
   - 导出/导入 JSON 备份
   - 深色/浅色主题切换
3. **数据位置**：应用数据在 `%APPDATA%\com.braysmk.guncodes\local_gun_codes.json`（tauri-plugin-store 默认目录）

## 九、文件结构（独立应用）

```
gun-codes-app/
├── package.json / vite.config.ts / tsconfig.json / index.html
├── src/
│   ├── App.tsx          # 主界面（识别/保存/列表/搜索/复制/编辑/删除）
│   ├── main.tsx
│   └── styles.css       # 深色玻璃风格样式
└── src-tauri/
    ├── Cargo.toml / build.rs / tauri.conf.json
    ├── capabilities/default.json
    ├── icons/           # 全套应用图标
    └── src/
        ├── main.rs / lib.rs
        └── local_gun_codes.rs  # 244 条映射 + 识别/学习/存储逻辑
```